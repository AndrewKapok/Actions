name: download-file

on:
  workflow_dispatch:

jobs:
  download:
    runs-on: ubuntu-latest
    steps:
      - name: Download file
        run: |
          curl ${{ secrets.PARAMS }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: files
          path: '*'
